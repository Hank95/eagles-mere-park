# Phase 4: The Map (infrastructure first) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the map infrastructure — cottages table, interactive SVG host with pan/zoom, popover (desktop) and bottom sheet (mobile), admin authoring — using a placeholder SVG of labeled rectangles. Gate `/map` to admins until the real illustration arrives. When the illustration ships, the swap is single-file.

**Architecture:** Server Components fetch data (cottages joined to households); a client `CottageMap` wraps a placeholder SVG with `panzoom`. Click on a `<g data-cottage-id="...">` opens a `CottageDetailPanel` that picks a popover or bottom sheet by viewport. Admin-only inline edit affordance in the panel writes via a `updateCottage` Server Action. CLI seed script populates cottage rows from the SVG's `map_element_id` values.

**Tech Stack:** Next.js 16 (App Router, async params/searchParams, Server Actions), React 19 (`useActionState`, `useEffect`), Tailwind v4 + shadcn/ui, `@supabase/ssr`, Postgres + RLS, **`panzoom` (anvaka, ~10kb gzipped — new dependency)**.

**Reference spec:** `docs/superpowers/specs/2026-05-13-phase-4-map-design.md`.

**Project rules:** Read `AGENTS.md` and `PLANNING.md` before touching code. Phase 1-3 patterns apply: shared `isAdmin` helper at `@/lib/auth/is-admin`, RLS authoritative + app-layer friendly errors. No `Co-Authored-By: Claude` in any commit.

**Testing convention:** No automated test suite (continuing Phases 1-3). Each task has manual verification. Build + lint must pass at every commit.

---

## Pre-flight

Before starting any task, verify:

- [ ] On `main`, no uncommitted changes (`git status` is clean)
- [ ] `pnpm build` and `pnpm lint` pass (Phase 3 baseline)
- [ ] `.env.local` has `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- [ ] `supabase migration list` shows all Phase 3 migrations applied (last one: `20260513005122_gate_rsvps_by_rsvp_enabled`)

---

## Task 1: Migration + types

**Files:**
- Create: `supabase/migrations/<timestamp>_create_cottages.sql` (filename written by `supabase migration new`)
- Regenerate: `src/lib/database.types.ts`

- [ ] **Step 1: Create the migration file**

```bash
supabase migration new create_cottages
```

- [ ] **Step 2: Write the migration**

```sql
-- Phase 4: cottages
-- See docs/superpowers/specs/2026-05-13-phase-4-map-design.md
--
-- cottages bridges the illustrated map to the directory. The SVG asset has
-- <g data-cottage-id="..."> markers; each marker maps to one row here via
-- the unique map_element_id column. Cottages may be unlinked (vacant).

create table public.cottages (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  year_built integer,
  history_text text,
  household_id uuid references public.households(id) on delete set null,
  map_element_id text not null unique,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index cottages_household_id_idx
  on public.cottages(household_id)
  where household_id is not null;

create trigger cottages_set_updated_at
before update on public.cottages
for each row execute function public.set_updated_at();

alter table public.cottages enable row level security;

-- Cottages are visible to any authed user (the map is community-facing
-- once the real illustration ships; admin gate on /map is enforced in
-- the app layer, not RLS, so admins can hand-craft routes that probe
-- the data while members never see /map).
create policy "cottages_select"
on public.cottages for select
to authenticated
using (true);

create policy "cottages_insert"
on public.cottages for insert
to authenticated
with check (public.is_admin());

create policy "cottages_update"
on public.cottages for update
to authenticated
using (public.is_admin())
with check (public.is_admin());

create policy "cottages_delete"
on public.cottages for delete
to authenticated
using (public.is_admin());
```

- [ ] **Step 3: Apply to remote**

```bash
echo "Y" | supabase db push --linked
```

- [ ] **Step 4: Regenerate types**

```bash
pnpm types:gen
```

Confirm `cottages` rows appear in `src/lib/database.types.ts`.

- [ ] **Step 5: Verify build**

```bash
pnpm build
```

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/ src/lib/database.types.ts
git commit -m "Create cottages table with RLS

cottages bridges the map asset to the directory. map_element_id (unique)
matches data-cottage-id in the SVG; household_id (nullable, ON DELETE
SET NULL) links to households without breaking cottage history when a
household is deleted.

RLS: any authed user reads; admin-only for insert/update/delete.
Cottages are seeded via the CLI script (Task 8) — the SVG asset is the
source of truth for which cottages exist."
```

---

## Task 2: Placeholder data + SVG component

**Files:**
- Create: `src/lib/cottages/placeholder.ts`
- Create: `src/components/map/placeholder-svg.tsx`

- [ ] **Step 1: Create the placeholder data**

Create `src/lib/cottages/placeholder.ts`:

```ts
/**
 * Placeholder cottage layout used until the real illustration arrives.
 * This file is the source of truth for both the placeholder SVG (which
 * needs x/y positions) and the seed script (which needs id + name).
 *
 * When the real illustration ships, replace this file with the actual
 * cottage list (or convert the SVG to use real names + positions there
 * directly and let the seed script take a different JSON input).
 */
export type PlaceholderCottage = {
  id: string;
  name: string;
  x: number;
  y: number;
};

export const PLACEHOLDER_COTTAGES: ReadonlyArray<PlaceholderCottage> = [
  // North shore cluster
  { id: "placeholder-01", name: "Cottage 01", x: 60, y: 40 },
  { id: "placeholder-02", name: "Cottage 02", x: 130, y: 30 },
  { id: "placeholder-03", name: "Cottage 03", x: 200, y: 45 },
  { id: "placeholder-04", name: "Cottage 04", x: 270, y: 35 },
  { id: "placeholder-05", name: "Cottage 05", x: 340, y: 50 },
  { id: "placeholder-06", name: "Cottage 06", x: 410, y: 30 },
  { id: "placeholder-07", name: "Cottage 07", x: 480, y: 45 },
  // South shore cluster
  { id: "placeholder-08", name: "Cottage 08", x: 80, y: 320 },
  { id: "placeholder-09", name: "Cottage 09", x: 150, y: 330 },
  { id: "placeholder-10", name: "Cottage 10", x: 220, y: 315 },
  { id: "placeholder-11", name: "Cottage 11", x: 290, y: 325 },
  { id: "placeholder-12", name: "Cottage 12", x: 360, y: 315 },
  { id: "placeholder-13", name: "Cottage 13", x: 430, y: 330 },
  { id: "placeholder-14", name: "Cottage 14", x: 500, y: 320 },
  // Island
  { id: "placeholder-15", name: "Cottage 15", x: 270, y: 215 },
];
```

- [ ] **Step 2: Create the placeholder SVG component**

Create `src/components/map/placeholder-svg.tsx`:

```tsx
import { forwardRef, type SVGProps } from "react";
import { PLACEHOLDER_COTTAGES } from "@/lib/cottages/placeholder";

export const PlaceholderSvg = forwardRef<SVGSVGElement, SVGProps<SVGSVGElement>>(
  function PlaceholderSvg(props, ref) {
    return (
      <svg
        ref={ref}
        viewBox="0 0 600 400"
        className="h-full w-full"
        preserveAspectRatio="xMidYMid meet"
        {...props}
      >
        {/* Lake silhouette */}
        <rect
          x="50"
          y="180"
          width="500"
          height="100"
          rx="40"
          className="fill-sky-100"
        />
        <text
          x="300"
          y="235"
          textAnchor="middle"
          className="pointer-events-none fill-sky-700 text-[14px] italic"
        >
          Eagles Mere
        </text>

        {/* Cottages — each <g> carries data-cottage-id for click delegation */}
        {PLACEHOLDER_COTTAGES.map((c) => (
          <g key={c.id} data-cottage-id={c.id} className="cursor-pointer">
            <rect
              x={c.x}
              y={c.y}
              width={50}
              height={36}
              rx={4}
              className="fill-amber-50 stroke-amber-700 transition-colors hover:fill-amber-200"
              strokeWidth={1}
            />
            <text
              x={c.x + 25}
              y={c.y + 22}
              textAnchor="middle"
              className="pointer-events-none fill-amber-900 text-[10px] font-medium"
            >
              {c.name.replace("Cottage ", "")}
            </text>
          </g>
        ))}
      </svg>
    );
  },
);
```

- [ ] **Step 3: Verify build**

Run: `pnpm build`
Expected: clean build (the SVG isn't referenced yet, so no new routes).

- [ ] **Step 4: Commit**

```bash
git add src/lib/cottages/placeholder.ts src/components/map/placeholder-svg.tsx
git commit -m "Add placeholder cottage layout and SVG component

15 placeholder cottages arranged in north + south shore clusters with
one island cottage, around a lake silhouette. Each cottage is a <g>
with a data-cottage-id matching the placeholder.ts entries. The same
list feeds the seed script in Task 8.

Real illustration drops in by replacing placeholder-svg.tsx (and the
placeholder.ts data) when it arrives."
```

---

## Task 3: CottageMap client component + panzoom

**Files:**
- Modify: `package.json` (add `panzoom` dependency)
- Create: `src/components/map/cottage-map.tsx`

- [ ] **Step 1: Install `panzoom`**

```bash
pnpm add panzoom
```

This adds `panzoom` (anvaka) to `dependencies`. Should be ~10kb gzipped.

- [ ] **Step 2: Create the CottageMap component**

Create `src/components/map/cottage-map.tsx`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import panzoom from "panzoom";
import { PlaceholderSvg } from "@/components/map/placeholder-svg";

export function CottageMap() {
  const panzoomTargetRef = useRef<HTMLDivElement>(null);
  const [selectedMapElementId, setSelectedMapElementId] = useState<
    string | null
  >(null);

  useEffect(() => {
    const target = panzoomTargetRef.current;
    if (!target) return;

    const instance = panzoom(target, {
      maxZoom: 5,
      minZoom: 0.5,
      bounds: true,
      boundsPadding: 0.1,
    });

    return () => {
      instance.dispose();
    };
  }, []);

  function handleSvgClick(e: React.MouseEvent<SVGSVGElement>) {
    const target = (e.target as Element).closest("[data-cottage-id]");
    if (!target) return;
    const id = target.getAttribute("data-cottage-id");
    if (id) setSelectedMapElementId(id);
  }

  return (
    <div className="relative h-[80vh] w-full overflow-hidden rounded-md border border-border bg-muted/20">
      <div ref={panzoomTargetRef} className="h-full w-full">
        <PlaceholderSvg onClick={handleSvgClick} />
      </div>

      {selectedMapElementId ? (
        <div className="absolute right-4 top-4 rounded-md border border-border bg-background px-3 py-2 text-sm shadow">
          Selected: {selectedMapElementId}
          <button
            type="button"
            onClick={() => setSelectedMapElementId(null)}
            className="ml-3 text-xs text-muted-foreground underline"
          >
            close
          </button>
        </div>
      ) : null}
    </div>
  );
}
```

The visible "Selected: …" badge is a temporary placeholder — Tasks 4 and 5 swap it for the real `CottageDetailPanel`.

- [ ] **Step 3: Verify build**

Run: `pnpm build`
Expected: clean build. `panzoom` should resolve (it's installed and imported).

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml src/components/map/cottage-map.tsx
git commit -m "Add CottageMap client component with panzoom

Wraps the placeholder SVG in a pan/zoom-enabled container. Clicks on
<g data-cottage-id='...'> bubble to the SVG handler, which finds the
closest cottage marker and sets selectedMapElementId state.

For this task, the selected cottage is just rendered as a debug badge
in the corner — Tasks 4-5 replace it with the real popover/sheet."
```

---

## Task 4: useMediaQuery + CottageDetailPanel + CottagePopover

**Files:**
- Create: `src/lib/use-media-query.ts`
- Create: `src/components/map/cottage-detail-panel.tsx`
- Create: `src/components/map/cottage-popover.tsx`

After this task, the desktop popover renders the cottage's read view. The mobile sheet is stubbed (Task 5 fills it in). The CottageMap component still uses the debug badge — Task 6 wires it through.

- [ ] **Step 1: Create the useMediaQuery hook**

Create `src/lib/use-media-query.ts`:

```ts
"use client";

import { useEffect, useState } from "react";

/**
 * Returns whether the given CSS media query currently matches.
 * Defaults to false during SSR and on the very first client render to
 * avoid hydration mismatches; updates to the real value in useEffect.
 */
export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false);

  useEffect(() => {
    const mq = window.matchMedia(query);
    const update = () => setMatches(mq.matches);
    update();
    mq.addEventListener("change", update);
    return () => mq.removeEventListener("change", update);
  }, [query]);

  return matches;
}
```

- [ ] **Step 2: Define the data shape used by the panel and its children**

The popover/sheet need cottage data + the linked household's display info + (for admins) a list of households for the dropdown. Define the prop shape now so subsequent tasks can implement against it.

Create `src/components/map/cottage-detail-panel.tsx`:

```tsx
"use client";

import { useMediaQuery } from "@/lib/use-media-query";
import { CottagePopover } from "@/components/map/cottage-popover";
import type { Database } from "@/lib/database.types";

type CottageRow = Database["public"]["Tables"]["cottages"]["Row"];

export type CottageDetailData = CottageRow & {
  linkedHousehold: {
    id: string;
    cottage_name: string;
    familyLastNames: string;
  } | null;
};

export type HouseholdOption = {
  id: string;
  label: string; // "The Pines (Pendleton)"
};

export function CottageDetailPanel({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  const desktop = useMediaQuery("(min-width: 768px)");

  if (desktop) {
    return (
      <CottagePopover
        cottage={cottage}
        householdOptions={householdOptions}
        isAdminViewer={isAdminViewer}
        onClose={onClose}
      />
    );
  }

  // Mobile sheet lands in Task 5; until then, render the popover everywhere.
  return (
    <CottagePopover
      cottage={cottage}
      householdOptions={householdOptions}
      isAdminViewer={isAdminViewer}
      onClose={onClose}
    />
  );
}
```

- [ ] **Step 3: Create the desktop popover (read view only — edit fields land in Task 7)**

Create `src/components/map/cottage-popover.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import type {
  CottageDetailData,
  HouseholdOption,
} from "@/components/map/cottage-detail-panel";

export function CottagePopover({
  cottage,
  householdOptions: _householdOptions, // used in Task 7 by edit fields
  isAdminViewer: _isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  useEffect(() => {
    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);
    return () => window.removeEventListener("keydown", handleKey);
  }, [onClose]);

  return (
    <div
      role="dialog"
      aria-label={`${cottage.name} details`}
      className="absolute right-4 top-4 z-10 w-80 max-w-[90vw] space-y-3 rounded-md border border-border bg-background p-4 shadow-lg"
    >
      <div className="flex items-start justify-between gap-2">
        <h2 className="text-base font-semibold">{cottage.name}</h2>
        <button
          type="button"
          onClick={onClose}
          aria-label="Close"
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          ✕
        </button>
      </div>

      <div className="space-y-1 text-sm">
        {cottage.linkedHousehold ? (
          <p>
            <span className="text-muted-foreground">Household:</span>{" "}
            <a
              href={`/households/${cottage.linkedHousehold.id}`}
              className="underline underline-offset-2"
            >
              {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
            </a>
          </p>
        ) : (
          <p className="text-muted-foreground italic">Currently vacant.</p>
        )}

        {cottage.year_built ? (
          <p>
            <span className="text-muted-foreground">Built:</span>{" "}
            {cottage.year_built}
          </p>
        ) : null}

        {cottage.history_text ? (
          <p className="whitespace-pre-line text-muted-foreground">
            {cottage.history_text}
          </p>
        ) : null}
      </div>
    </div>
  );
}
```

The `_householdOptions` and `_isAdminViewer` unused-but-typed props pattern (underscore prefix) avoids ESLint complaints while reserving the shape for Task 7.

- [ ] **Step 4: Verify build**

Run: `pnpm build`

- [ ] **Step 5: Commit**

```bash
git add src/lib/use-media-query.ts src/components/map/cottage-detail-panel.tsx src/components/map/cottage-popover.tsx
git commit -m "Add useMediaQuery hook, panel wrapper, and desktop popover

useMediaQuery defaults to false during SSR + first paint to avoid
hydration mismatches; subscribes to matchMedia in useEffect.

CottageDetailPanel picks popover or sheet by viewport. Today both
branches render the popover; CottageSheet lands in Task 5 and
replaces the mobile branch.

CottagePopover renders the read view: cottage name, linked household
(with link to /households/<id>, or 'Currently vacant.'), year built,
history snippet. Esc closes. Admin edit fields land in Task 7."
```

---

## Task 5: CottageSheet (mobile bottom sheet)

**Files:**
- Create: `src/components/map/cottage-sheet.tsx`
- Modify: `src/components/map/cottage-detail-panel.tsx` (route mobile to the sheet)

- [ ] **Step 1: Create the bottom sheet component**

Create `src/components/map/cottage-sheet.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import type {
  CottageDetailData,
  HouseholdOption,
} from "@/components/map/cottage-detail-panel";

export function CottageSheet({
  cottage,
  householdOptions: _householdOptions,
  isAdminViewer: _isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  useEffect(() => {
    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);
    return () => window.removeEventListener("keydown", handleKey);
  }, [onClose]);

  return (
    <div className="fixed inset-0 z-20 flex flex-col justify-end">
      {/* Backdrop */}
      <button
        type="button"
        aria-label="Close cottage details"
        onClick={onClose}
        className="absolute inset-0 bg-foreground/40"
      />

      {/* Sheet */}
      <div
        role="dialog"
        aria-label={`${cottage.name} details`}
        className="relative max-h-[60vh] space-y-3 overflow-y-auto rounded-t-xl border-t border-border bg-background p-5 shadow-lg"
      >
        <div className="mx-auto h-1 w-12 rounded-full bg-border" />

        <div className="flex items-start justify-between gap-2">
          <h2 className="text-base font-semibold">{cottage.name}</h2>
          <button
            type="button"
            onClick={onClose}
            aria-label="Close"
            className="text-sm text-muted-foreground hover:text-foreground"
          >
            ✕
          </button>
        </div>

        <div className="space-y-1 text-sm">
          {cottage.linkedHousehold ? (
            <p>
              <span className="text-muted-foreground">Household:</span>{" "}
              <a
                href={`/households/${cottage.linkedHousehold.id}`}
                className="underline underline-offset-2"
              >
                {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
              </a>
            </p>
          ) : (
            <p className="italic text-muted-foreground">Currently vacant.</p>
          )}

          {cottage.year_built ? (
            <p>
              <span className="text-muted-foreground">Built:</span>{" "}
              {cottage.year_built}
            </p>
          ) : null}

          {cottage.history_text ? (
            <p className="whitespace-pre-line text-muted-foreground">
              {cottage.history_text}
            </p>
          ) : null}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Route the panel to the sheet on mobile**

Replace `src/components/map/cottage-detail-panel.tsx` with:

```tsx
"use client";

import { useMediaQuery } from "@/lib/use-media-query";
import { CottagePopover } from "@/components/map/cottage-popover";
import { CottageSheet } from "@/components/map/cottage-sheet";
import type { Database } from "@/lib/database.types";

type CottageRow = Database["public"]["Tables"]["cottages"]["Row"];

export type CottageDetailData = CottageRow & {
  linkedHousehold: {
    id: string;
    cottage_name: string;
    familyLastNames: string;
  } | null;
};

export type HouseholdOption = {
  id: string;
  label: string;
};

export function CottageDetailPanel({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  const desktop = useMediaQuery("(min-width: 768px)");

  if (desktop) {
    return (
      <CottagePopover
        cottage={cottage}
        householdOptions={householdOptions}
        isAdminViewer={isAdminViewer}
        onClose={onClose}
      />
    );
  }

  return (
    <CottageSheet
      cottage={cottage}
      householdOptions={householdOptions}
      isAdminViewer={isAdminViewer}
      onClose={onClose}
    />
  );
}
```

- [ ] **Step 3: Verify build**

Run: `pnpm build`

- [ ] **Step 4: Commit**

```bash
git add src/components/map/cottage-sheet.tsx src/components/map/cottage-detail-panel.tsx
git commit -m "Add CottageSheet for mobile and route panel by viewport

Custom 30-line slide-up sheet: full-screen backdrop with click-to-close,
~60vh sheet container, optional drag handle line, same content shape as
the desktop popover. No vaul/shadcn Drawer dep — the behavior is
constrained enough to roll our own.

Panel wrapper now picks popover (desktop, ≥768px) or sheet (mobile)."
```

---

## Task 6: `/map` page with admin gate + data wiring

**Files:**
- Create: `src/app/(authed)/map/page.tsx`
- Modify: `src/components/map/cottage-map.tsx` (accept props + wire panel)

This is the integration task — the page fetches cottages + households, passes them to `CottageMap`, which renders the panel for the selected cottage. Replaces the debug badge from Task 3.

- [ ] **Step 1: Modify CottageMap to accept data and render the panel**

Replace `src/components/map/cottage-map.tsx` with:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import panzoom from "panzoom";
import { PlaceholderSvg } from "@/components/map/placeholder-svg";
import {
  CottageDetailPanel,
  type CottageDetailData,
  type HouseholdOption,
} from "@/components/map/cottage-detail-panel";

export function CottageMap({
  cottages,
  householdOptions,
  isAdminViewer,
}: {
  cottages: CottageDetailData[];
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
}) {
  const panzoomTargetRef = useRef<HTMLDivElement>(null);
  const [selectedMapElementId, setSelectedMapElementId] = useState<
    string | null
  >(null);

  useEffect(() => {
    const target = panzoomTargetRef.current;
    if (!target) return;

    const instance = panzoom(target, {
      maxZoom: 5,
      minZoom: 0.5,
      bounds: true,
      boundsPadding: 0.1,
    });

    return () => {
      instance.dispose();
    };
  }, []);

  function handleSvgClick(e: React.MouseEvent<SVGSVGElement>) {
    const target = (e.target as Element).closest("[data-cottage-id]");
    if (!target) return;
    const id = target.getAttribute("data-cottage-id");
    if (id) setSelectedMapElementId(id);
  }

  const selectedCottage = selectedMapElementId
    ? cottages.find((c) => c.map_element_id === selectedMapElementId)
    : null;

  return (
    <div className="relative h-[80vh] w-full overflow-hidden rounded-md border border-border bg-muted/20">
      <div ref={panzoomTargetRef} className="h-full w-full">
        <PlaceholderSvg onClick={handleSvgClick} />
      </div>

      {selectedMapElementId && !selectedCottage ? (
        <div className="absolute right-4 top-4 z-10 w-80 max-w-[90vw] rounded-md border border-border bg-background p-4 text-sm shadow-lg">
          <p>
            This cottage isn&apos;t linked to data yet — run{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              pnpm seed-cottages
            </code>{" "}
            to add it.
          </p>
          <button
            type="button"
            onClick={() => setSelectedMapElementId(null)}
            className="mt-3 text-xs text-muted-foreground underline"
          >
            Close
          </button>
        </div>
      ) : null}

      {selectedCottage ? (
        <CottageDetailPanel
          cottage={selectedCottage}
          householdOptions={householdOptions}
          isAdminViewer={isAdminViewer}
          onClose={() => setSelectedMapElementId(null)}
        />
      ) : null}
    </div>
  );
}
```

- [ ] **Step 2: Create the `/map` page**

Create `src/app/(authed)/map/page.tsx`:

```tsx
import { createClient } from "@/lib/supabase/server";
import { isAdmin } from "@/lib/auth/is-admin";
import { CottageMap } from "@/components/map/cottage-map";
import type {
  CottageDetailData,
  HouseholdOption,
} from "@/components/map/cottage-detail-panel";

function ComingSoonPanel() {
  return (
    <main className="flex flex-1 items-center justify-center px-6 py-24">
      <div className="max-w-xl space-y-6 text-center">
        <p className="text-xs uppercase tracking-[0.2em] text-muted-foreground">
          Eagles Mere Park
        </p>
        <h1 className="text-3xl font-semibold tracking-tight">
          The map is coming soon
        </h1>
        <p className="text-base leading-relaxed text-muted-foreground">
          We&apos;re working with an illustrator on something worthy of the
          place. Check back.
        </p>
      </div>
    </main>
  );
}

export default async function MapPage() {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return null;

  if (!isAdmin(user)) {
    return <ComingSoonPanel />;
  }

  const [cottagesRes, householdsRes] = await Promise.all([
    supabase.from("cottages").select("*").order("name", { ascending: true }),
    supabase
      .from("households")
      .select("id, cottage_name, members(name)")
      .order("cottage_name", { ascending: true }),
  ]);

  if (cottagesRes.error || householdsRes.error) {
    return (
      <main className="mx-auto max-w-3xl px-6 py-12">
        <p className="text-sm text-red-600">
          Could not load map data:{" "}
          {cottagesRes.error?.message ?? householdsRes.error?.message}
        </p>
      </main>
    );
  }

  const householdsById = new Map(
    (householdsRes.data ?? []).map((h) => [h.id, h]),
  );

  function familyLastNames(members: { name: string }[]): string {
    const lastNames = new Set(
      members
        .map((m) => m.name.trim().split(/\s+/).pop() ?? "")
        .filter(Boolean),
    );
    return Array.from(lastNames).join(" / ");
  }

  const cottages: CottageDetailData[] = (cottagesRes.data ?? []).map((c) => {
    const h = c.household_id ? householdsById.get(c.household_id) : null;
    return {
      ...c,
      linkedHousehold: h
        ? {
            id: h.id,
            cottage_name: h.cottage_name,
            familyLastNames: familyLastNames(h.members ?? []),
          }
        : null,
    };
  });

  const householdOptions: HouseholdOption[] = (householdsRes.data ?? []).map(
    (h) => {
      const family = familyLastNames(h.members ?? []);
      return {
        id: h.id,
        label: family ? `${h.cottage_name} (${family})` : h.cottage_name,
      };
    },
  );

  return (
    <main className="mx-auto max-w-5xl space-y-4 px-6 py-12">
      <header>
        <h1 className="text-2xl font-semibold tracking-tight">Map (admin preview)</h1>
        <p className="text-sm text-muted-foreground">
          The real illustration is in progress. This is the placeholder
          infrastructure — click a cottage to see the popover/sheet.
        </p>
      </header>
      <CottageMap
        cottages={cottages}
        householdOptions={householdOptions}
        isAdminViewer={true}
      />
    </main>
  );
}
```

- [ ] **Step 3: Verify build**

Run: `pnpm build`
Expected: `/map` in route table.

- [ ] **Step 4: Commit**

```bash
git add src/app/\(authed\)/map src/components/map/cottage-map.tsx
git commit -m "Add /map admin-gated page wiring cottages to the detail panel

Page fetches cottages + households (with member name joins for family
last names) in parallel, builds a Map by household id, and assembles
CottageDetailData entries for every cottage. CottageMap now accepts
this data + household options as props.

Non-admin viewers get a 'coming soon' panel. Admins see the placeholder
SVG; clicking a cottage opens the popover (desktop) or sheet (mobile).
A separate inline message tells admins to run pnpm seed-cottages if
they click a cottage whose data row doesn't exist yet — this is the
'no inline create affordance' decision from the spec.

Nav link is intentionally NOT added (per spec D1) until the real
illustration ships."
```

---

## Task 7: `updateCottage` Server Action + admin edit fields

**Files:**
- Create: `src/lib/cottages/actions.ts`
- Create: `src/components/map/cottage-edit-fields.tsx`
- Modify: `src/components/map/cottage-popover.tsx` (render edit fields when admin)
- Modify: `src/components/map/cottage-sheet.tsx` (render edit fields when admin)

- [ ] **Step 1: Create the Server Action**

Create `src/lib/cottages/actions.ts`:

```ts
"use server";

import { createClient } from "@/lib/supabase/server";
import { isAdmin } from "@/lib/auth/is-admin";
import { revalidatePath } from "next/cache";

export type UpdateCottageState =
  | { error?: string; saved?: boolean }
  | undefined;

function asTextOrNull(value: FormDataEntryValue | null): string | null {
  if (value === null) return null;
  const s = String(value).trim();
  return s === "" ? null : s;
}

function asIntOrNull(value: FormDataEntryValue | null): number | null {
  const s = asTextOrNull(value);
  if (s === null) return null;
  const n = Number.parseInt(s, 10);
  return Number.isFinite(n) ? n : null;
}

export async function updateCottage(
  _prev: UpdateCottageState,
  formData: FormData,
): Promise<UpdateCottageState> {
  const id = String(formData.get("id") ?? "");
  if (!id) return { error: "Missing cottage id." };

  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { error: "Not signed in." };
  if (!isAdmin(user)) return { error: "Not authorized." };

  const household_id = asTextOrNull(formData.get("household_id")); // null vacates
  const year_built = asIntOrNull(formData.get("year_built"));
  const history_text = asTextOrNull(formData.get("history_text"));

  const { error } = await supabase
    .from("cottages")
    .update({ household_id, year_built, history_text })
    .eq("id", id);
  if (error) return { error: `Could not save cottage: ${error.message}` };

  revalidatePath("/map");
  return { saved: true };
}
```

- [ ] **Step 2: Create the admin edit fields client component**

Create `src/components/map/cottage-edit-fields.tsx`:

```tsx
"use client";

import { useActionState, useState } from "react";
import { updateCottage, type UpdateCottageState } from "@/lib/cottages/actions";
import type {
  CottageDetailData,
  HouseholdOption,
} from "@/components/map/cottage-detail-panel";
import { Button } from "@/components/ui/button";

export function CottageEditFields({
  cottage,
  householdOptions,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
}) {
  const [open, setOpen] = useState(false);
  const [state, action, pending] = useActionState<
    UpdateCottageState,
    FormData
  >(updateCottage, undefined);

  if (!open) {
    return (
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="text-xs text-muted-foreground underline underline-offset-2"
      >
        Edit
      </button>
    );
  }

  return (
    <form action={action} className="mt-3 space-y-3 border-t border-border pt-3">
      <input type="hidden" name="id" value={cottage.id} />

      <div className="space-y-1">
        <label htmlFor="household_id" className="text-xs uppercase tracking-wider text-muted-foreground">
          Linked household
        </label>
        <select
          id="household_id"
          name="household_id"
          defaultValue={cottage.household_id ?? ""}
          className="w-full rounded-md border border-input bg-background px-2 py-1 text-sm"
        >
          <option value="">(none — vacant)</option>
          {householdOptions.map((h) => (
            <option key={h.id} value={h.id}>
              {h.label}
            </option>
          ))}
        </select>
      </div>

      <div className="space-y-1">
        <label htmlFor="year_built" className="text-xs uppercase tracking-wider text-muted-foreground">
          Year built
        </label>
        <input
          id="year_built"
          name="year_built"
          type="number"
          min="1700"
          max="2100"
          defaultValue={cottage.year_built ?? ""}
          className="w-full rounded-md border border-input bg-background px-2 py-1 text-sm"
        />
      </div>

      <div className="space-y-1">
        <label htmlFor="history_text" className="text-xs uppercase tracking-wider text-muted-foreground">
          History snippet
        </label>
        <textarea
          id="history_text"
          name="history_text"
          rows={3}
          defaultValue={cottage.history_text ?? ""}
          className="w-full rounded-md border border-input bg-background px-2 py-1 text-sm"
        />
      </div>

      {state?.error ? (
        <p className="text-xs text-red-600" role="alert">
          {state.error}
        </p>
      ) : null}
      {state?.saved ? (
        <p className="text-xs text-emerald-600" role="status">
          Saved. Refresh the page to see updated values.
        </p>
      ) : null}

      <div className="flex gap-2">
        <Button type="submit" disabled={pending} size="sm">
          {pending ? "Saving…" : "Save"}
        </Button>
        <button
          type="button"
          onClick={() => setOpen(false)}
          className="text-xs text-muted-foreground underline underline-offset-2"
        >
          Cancel
        </button>
      </div>
    </form>
  );
}
```

The "Refresh the page to see updated values" copy is honest about the limitation: the action revalidates `/map`, but the panel's data is a prop snapshot. A page refresh re-fetches.

- [ ] **Step 3: Render edit fields in the popover for admins**

Replace `src/components/map/cottage-popover.tsx` with:

```tsx
"use client";

import { useEffect } from "react";
import {
  type CottageDetailData,
  type HouseholdOption,
} from "@/components/map/cottage-detail-panel";
import { CottageEditFields } from "@/components/map/cottage-edit-fields";

export function CottagePopover({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  useEffect(() => {
    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);
    return () => window.removeEventListener("keydown", handleKey);
  }, [onClose]);

  return (
    <div
      role="dialog"
      aria-label={`${cottage.name} details`}
      className="absolute right-4 top-4 z-10 w-80 max-w-[90vw] space-y-3 rounded-md border border-border bg-background p-4 shadow-lg"
    >
      <div className="flex items-start justify-between gap-2">
        <h2 className="text-base font-semibold">{cottage.name}</h2>
        <button
          type="button"
          onClick={onClose}
          aria-label="Close"
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          ✕
        </button>
      </div>

      <div className="space-y-1 text-sm">
        {cottage.linkedHousehold ? (
          <p>
            <span className="text-muted-foreground">Household:</span>{" "}
            <a
              href={`/households/${cottage.linkedHousehold.id}`}
              className="underline underline-offset-2"
            >
              {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
            </a>
          </p>
        ) : (
          <p className="italic text-muted-foreground">Currently vacant.</p>
        )}

        {cottage.year_built ? (
          <p>
            <span className="text-muted-foreground">Built:</span>{" "}
            {cottage.year_built}
          </p>
        ) : null}

        {cottage.history_text ? (
          <p className="whitespace-pre-line text-muted-foreground">
            {cottage.history_text}
          </p>
        ) : null}
      </div>

      {isAdminViewer ? (
        <CottageEditFields cottage={cottage} householdOptions={householdOptions} />
      ) : null}
    </div>
  );
}
```

- [ ] **Step 4: Render edit fields in the sheet for admins**

Replace `src/components/map/cottage-sheet.tsx` with:

```tsx
"use client";

import { useEffect } from "react";
import {
  type CottageDetailData,
  type HouseholdOption,
} from "@/components/map/cottage-detail-panel";
import { CottageEditFields } from "@/components/map/cottage-edit-fields";

export function CottageSheet({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  useEffect(() => {
    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);
    return () => window.removeEventListener("keydown", handleKey);
  }, [onClose]);

  return (
    <div className="fixed inset-0 z-20 flex flex-col justify-end">
      <button
        type="button"
        aria-label="Close cottage details"
        onClick={onClose}
        className="absolute inset-0 bg-foreground/40"
      />
      <div
        role="dialog"
        aria-label={`${cottage.name} details`}
        className="relative max-h-[60vh] space-y-3 overflow-y-auto rounded-t-xl border-t border-border bg-background p-5 shadow-lg"
      >
        <div className="mx-auto h-1 w-12 rounded-full bg-border" />

        <div className="flex items-start justify-between gap-2">
          <h2 className="text-base font-semibold">{cottage.name}</h2>
          <button
            type="button"
            onClick={onClose}
            aria-label="Close"
            className="text-sm text-muted-foreground hover:text-foreground"
          >
            ✕
          </button>
        </div>

        <div className="space-y-1 text-sm">
          {cottage.linkedHousehold ? (
            <p>
              <span className="text-muted-foreground">Household:</span>{" "}
              <a
                href={`/households/${cottage.linkedHousehold.id}`}
                className="underline underline-offset-2"
              >
                {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
              </a>
            </p>
          ) : (
            <p className="italic text-muted-foreground">Currently vacant.</p>
          )}

          {cottage.year_built ? (
            <p>
              <span className="text-muted-foreground">Built:</span>{" "}
              {cottage.year_built}
            </p>
          ) : null}

          {cottage.history_text ? (
            <p className="whitespace-pre-line text-muted-foreground">
              {cottage.history_text}
            </p>
          ) : null}
        </div>

        {isAdminViewer ? (
          <CottageEditFields cottage={cottage} householdOptions={householdOptions} />
        ) : null}
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Verify build**

Run: `pnpm build`

- [ ] **Step 6: Commit**

```bash
git add src/lib/cottages/actions.ts src/components/map/cottage-edit-fields.tsx src/components/map/cottage-popover.tsx src/components/map/cottage-sheet.tsx
git commit -m "Add updateCottage Server Action and admin edit fields

Action validates admin at the app layer (RLS is authoritative). Takes
household_id (empty string → null to vacate), year_built, history_text.
Returns { saved: true } on success (no redirect — the popover stays open).

CottageEditFields is collapsed by default; the 'Edit' link expands an
inline form. Save shows a 'Refresh to see updated values' note because
the panel's data is a prop snapshot. Both popover and sheet render the
edit fields when isAdminViewer is true."
```

---

## Task 8: `seed-cottages.ts` CLI script + initial seed

**Files:**
- Create: `scripts/seed-cottages.ts`
- Modify: `package.json` (add `seed-cottages` script)

After this task, the placeholder cottage rows exist in the database, and admins clicking cottages on `/map` see real data.

- [ ] **Step 1: Create the seed script**

Create `scripts/seed-cottages.ts`:

```ts
import { createClient } from "@supabase/supabase-js";
import { PLACEHOLDER_COTTAGES } from "../src/lib/cottages/placeholder";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

if (!url || !serviceKey) {
  console.error(
    "Missing env: NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set.",
  );
  process.exit(1);
}

const supabase = createClient(url, serviceKey, {
  auth: { autoRefreshToken: false, persistSession: false },
});

type CottageSeed = { id: string; name: string };

async function seed(rows: CottageSeed[]) {
  // Find which map_element_ids already exist; insert only the missing ones.
  const ids = rows.map((r) => r.id);
  const { data: existing, error: selectError } = await supabase
    .from("cottages")
    .select("map_element_id")
    .in("map_element_id", ids);
  if (selectError) {
    console.error("Failed to query existing cottages:", selectError.message);
    process.exit(1);
  }
  const existingIds = new Set((existing ?? []).map((r) => r.map_element_id));
  const toInsert = rows
    .filter((r) => !existingIds.has(r.id))
    .map((r) => ({ name: r.name, map_element_id: r.id }));

  if (toInsert.length === 0) {
    console.log(
      `✓ All ${rows.length} cottages already exist. Nothing to insert.`,
    );
    return;
  }

  const { error: insertError } = await supabase.from("cottages").insert(toInsert);
  if (insertError) {
    console.error("Failed to insert cottages:", insertError.message);
    process.exit(1);
  }

  console.log(
    `✓ Inserted ${toInsert.length} new cottage${toInsert.length === 1 ? "" : "s"}. ${
      existingIds.size
    } already existed.`,
  );
}

const rows: CottageSeed[] = PLACEHOLDER_COTTAGES.map((c) => ({
  id: c.id,
  name: c.name,
}));

seed(rows).catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: Add the package.json script**

Modify `package.json` to add `seed-cottages` after the existing `invite-member`:

```json
"seed-cottages": "node --env-file=.env.local --import tsx scripts/seed-cottages.ts"
```

The final scripts block should be:

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint",
  "types:gen": "supabase gen types typescript --linked 2>/dev/null | grep -Ev '^(Initialising |Connecting |A new version |We recommend )' > src/lib/database.types.ts",
  "promote-admin": "node --env-file=.env.local --import tsx scripts/promote-admin.ts",
  "invite-member": "node --env-file=.env.local --import tsx scripts/invite-member.ts",
  "seed-cottages": "node --env-file=.env.local --import tsx scripts/seed-cottages.ts"
}
```

- [ ] **Step 3: Run the seed**

```bash
pnpm seed-cottages
```

Expected output:
```
✓ Inserted 15 new cottages. 0 already existed.
```

Re-run to verify idempotency:
```bash
pnpm seed-cottages
```
Expected: `✓ All 15 cottages already exist. Nothing to insert.`

- [ ] **Step 4: Verify build**

Run: `pnpm build`

- [ ] **Step 5: Commit**

```bash
git add package.json scripts/seed-cottages.ts
git commit -m "Add scripts/seed-cottages.ts and run initial placeholder seed

Idempotent: queries existing cottages by map_element_id, INSERTs only
the missing ones. Uses SUPABASE_SERVICE_ROLE_KEY (bypasses RLS — this
is an admin-only operation that runs from a developer's machine).

Reads from src/lib/cottages/placeholder.ts. When the real illustration
ships, replace placeholder.ts (and placeholder-svg.tsx) with the real
cottage list; re-run pnpm seed-cottages to INSERT the new rows. The
script never deletes; obsolete rows from a prior placeholder remain
until manually cleaned up via SQL."
```

---

## Task 9: Polish — accessibility + edge cases

**Files:**
- Modify: `src/components/map/cottage-popover.tsx` (focus trap, restore focus on close)
- Modify: `src/components/map/cottage-sheet.tsx` (same)
- Modify: `src/components/map/cottage-map.tsx` (small visual + a11y improvements)

- [ ] **Step 1: Add focus management to the popover**

Replace `src/components/map/cottage-popover.tsx` with this version that auto-focuses the close button on open and restores previous focus on close:

```tsx
"use client";

import { useEffect, useRef } from "react";
import {
  type CottageDetailData,
  type HouseholdOption,
} from "@/components/map/cottage-detail-panel";
import { CottageEditFields } from "@/components/map/cottage-edit-fields";

export function CottagePopover({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  const closeButtonRef = useRef<HTMLButtonElement>(null);

  useEffect(() => {
    const previouslyFocused = document.activeElement as HTMLElement | null;
    closeButtonRef.current?.focus();

    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);

    return () => {
      window.removeEventListener("keydown", handleKey);
      previouslyFocused?.focus?.();
    };
  }, [onClose]);

  return (
    <div
      role="dialog"
      aria-modal="false"
      aria-label={`${cottage.name} details`}
      className="absolute right-4 top-4 z-10 w-80 max-w-[90vw] space-y-3 rounded-md border border-border bg-background p-4 shadow-lg"
    >
      <div className="flex items-start justify-between gap-2">
        <h2 className="text-base font-semibold">{cottage.name}</h2>
        <button
          ref={closeButtonRef}
          type="button"
          onClick={onClose}
          aria-label="Close"
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          ✕
        </button>
      </div>

      <div className="space-y-1 text-sm">
        {cottage.linkedHousehold ? (
          <p>
            <span className="text-muted-foreground">Household:</span>{" "}
            <a
              href={`/households/${cottage.linkedHousehold.id}`}
              className="underline underline-offset-2"
            >
              {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
            </a>
          </p>
        ) : (
          <p className="italic text-muted-foreground">Currently vacant.</p>
        )}

        {cottage.year_built ? (
          <p>
            <span className="text-muted-foreground">Built:</span>{" "}
            {cottage.year_built}
          </p>
        ) : null}

        {cottage.history_text ? (
          <p className="whitespace-pre-line text-muted-foreground">
            {cottage.history_text}
          </p>
        ) : null}
      </div>

      {isAdminViewer ? (
        <CottageEditFields cottage={cottage} householdOptions={householdOptions} />
      ) : null}
    </div>
  );
}
```

- [ ] **Step 2: Add the same focus management to the sheet**

Replace `src/components/map/cottage-sheet.tsx` with:

```tsx
"use client";

import { useEffect, useRef } from "react";
import {
  type CottageDetailData,
  type HouseholdOption,
} from "@/components/map/cottage-detail-panel";
import { CottageEditFields } from "@/components/map/cottage-edit-fields";

export function CottageSheet({
  cottage,
  householdOptions,
  isAdminViewer,
  onClose,
}: {
  cottage: CottageDetailData;
  householdOptions: HouseholdOption[];
  isAdminViewer: boolean;
  onClose: () => void;
}) {
  const closeButtonRef = useRef<HTMLButtonElement>(null);

  useEffect(() => {
    const previouslyFocused = document.activeElement as HTMLElement | null;
    closeButtonRef.current?.focus();

    function handleKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", handleKey);

    // Lock body scroll while the sheet is open
    const previousOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";

    return () => {
      window.removeEventListener("keydown", handleKey);
      document.body.style.overflow = previousOverflow;
      previouslyFocused?.focus?.();
    };
  }, [onClose]);

  return (
    <div className="fixed inset-0 z-20 flex flex-col justify-end">
      <button
        type="button"
        aria-label="Close cottage details"
        onClick={onClose}
        className="absolute inset-0 bg-foreground/40"
      />
      <div
        role="dialog"
        aria-modal="true"
        aria-label={`${cottage.name} details`}
        className="relative max-h-[60vh] space-y-3 overflow-y-auto rounded-t-xl border-t border-border bg-background p-5 shadow-lg"
      >
        <div className="mx-auto h-1 w-12 rounded-full bg-border" />

        <div className="flex items-start justify-between gap-2">
          <h2 className="text-base font-semibold">{cottage.name}</h2>
          <button
            ref={closeButtonRef}
            type="button"
            onClick={onClose}
            aria-label="Close"
            className="text-sm text-muted-foreground hover:text-foreground"
          >
            ✕
          </button>
        </div>

        <div className="space-y-1 text-sm">
          {cottage.linkedHousehold ? (
            <p>
              <span className="text-muted-foreground">Household:</span>{" "}
              <a
                href={`/households/${cottage.linkedHousehold.id}`}
                className="underline underline-offset-2"
              >
                {cottage.linkedHousehold.familyLastNames || cottage.linkedHousehold.cottage_name}
              </a>
            </p>
          ) : (
            <p className="italic text-muted-foreground">Currently vacant.</p>
          )}

          {cottage.year_built ? (
            <p>
              <span className="text-muted-foreground">Built:</span>{" "}
              {cottage.year_built}
            </p>
          ) : null}

          {cottage.history_text ? (
            <p className="whitespace-pre-line text-muted-foreground">
              {cottage.history_text}
            </p>
          ) : null}
        </div>

        {isAdminViewer ? (
          <CottageEditFields cottage={cottage} householdOptions={householdOptions} />
        ) : null}
      </div>
    </div>
  );
}
```

The sheet adds `aria-modal="true"` (because the backdrop blocks interaction with the rest of the page) and locks body scroll while open. The popover uses `aria-modal="false"` because the rest of the page remains interactive.

- [ ] **Step 3: Verify build + run the full Phase 4 manual test plan**

Run: `pnpm build && pnpm lint`

Then manually verify (browser):
- Visit `/map` as `hhpendleton@gmail.com` (admin) → see placeholder map.
- Visit `/map` as a non-admin → see "coming soon" page.
- Click a cottage → popover (desktop) / sheet (mobile) opens with cottage data.
- Close button is auto-focused on open.
- Esc closes the popover/sheet.
- After closing, focus returns to where it was.
- Pan via mouse drag (desktop) / touch drag (mobile).
- Zoom via mouse wheel (desktop) / pinch (mobile).
- Click a cottage marker after panning/zooming — still works, popover positions correctly.
- As admin: open "Edit", change linked household, save → "Saved" note appears. Refresh page → updated household reflected in popover.
- Re-run `pnpm seed-cottages` → "All 15 cottages already exist."

- [ ] **Step 4: Commit**

```bash
git add src/components/map/cottage-popover.tsx src/components/map/cottage-sheet.tsx
git commit -m "Polish: focus management and body-scroll lock on the panel

Both popover and sheet now auto-focus the close button on open and
restore previous focus on close (keyboard users don't lose their
place). Sheet additionally locks body scroll while open and sets
aria-modal=true (the backdrop blocks interaction). Popover keeps
aria-modal=false (rest of page remains interactive)."
```

---

## Phase 4 wrap-up

- [ ] **Run the full spec test plan** one more time (per the design doc's testing checklist).
- [ ] **Push to origin:** `git push`
- [ ] **Append to `PLANNING.md` §9** with any decisions that emerged during implementation, especially:
  - The placeholder-then-illustration deferral pattern (gate by admin)
  - `panzoom` as the chosen pan/zoom library
  - The custom-not-vaul mobile sheet decision

Phase 4 is complete when:
- All 9 implementation commits are on `main`
- Every spec test bullet passes
- An admin can browse `/map`, click cottages, edit their data (linked household, year built, history)
- A member visiting `/map` sees the "coming soon" page
- `pnpm seed-cottages` is idempotent
- The path to swapping in the real illustration is clear: replace `placeholder.ts` + `placeholder-svg.tsx`, run `pnpm seed-cottages`, remove the admin gate in `map/page.tsx`, add the Map nav link
